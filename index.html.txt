<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">

    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">

    <title>SYNERGY AI</title>

    <style>

        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #030712;
            color: white;
            overflow: hidden;
        }

        /* =========================
           MAIN SCREEN
        ========================= */

        .container {
            width: 100vw;
            height: 100vh;
            position: relative;
            overflow: hidden;

            background:
                radial-gradient(
                    circle at 50% 60%,
                    rgba(40, 90, 255, 0.18),
                    transparent 35%
                ),
                radial-gradient(
                    circle at 10% 20%,
                    rgba(70, 120, 255, 0.12),
                    transparent 25%
                ),
                radial-gradient(
                    circle at 90% 20%,
                    rgba(120, 70, 255, 0.10),
                    transparent 25%
                ),
                #030712;
        }

        /* =========================
           STARS / PARTICLES
        ========================= */

        .particle {
            position: absolute;

            width: 4px;
            height: 4px;

            background: #7aa2ff;

            border-radius: 50%;

            box-shadow:
                0 0 10px #4f7cff,
                0 0 20px #4f7cff;

            animation: twinkle 3s infinite ease-in-out;
        }

        @keyframes twinkle {

            0%, 100% {
                opacity: 0.2;
                transform: scale(0.7);
            }

            50% {
                opacity: 1;
                transform: scale(1.4);
            }

        }

        /* =========================
           STADIUM LIGHTS
        ========================= */

        .light {
            position: absolute;

            width: 180px;
            height: 180px;

            background: radial-gradient(
                circle,
                rgba(120, 180, 255, 0.9),
                rgba(100, 150, 255, 0.2),
                transparent 70%
            );

            filter: blur(8px);

            opacity: 0.7;
        }

        .light-left {
            left: -50px;
            top: 100px;
        }

        .light-right {
            right: -50px;
            top: 100px;
        }

        /* =========================
           HEADER
        ========================= */

        .header {
            position: absolute;

            top: 35px;
            left: 55px;
            right: 55px;

            display: flex;
            justify-content: space-between;
            align-items: center;

            z-index: 10;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            font-size: 42px;
            color: #9ec5ff;
        }

        .logo-text {
            font-size: 26px;
            font-weight: 800;

            letter-spacing: 10px;
        }

        .logo-sub {
            font-size: 11px;

            letter-spacing: 7px;

            color: #8a93a8;

            margin-top: 5px;
        }

        .ai-status {

            display: flex;

            align-items: center;

            gap: 15px;

            padding: 14px 22px;

            border: 1px solid rgba(130, 160, 255, 0.3);

            border-radius: 15px;

            background: rgba(255,255,255,0.03);

            backdrop-filter: blur(10px);

            font-size: 12px;

            letter-spacing: 2px;

            color: #cbd5e1;
        }

        .status-dot {

            width: 8px;
            height: 8px;

            background: #4f8cff;

            border-radius: 50%;

            box-shadow: 0 0 15px #4f8cff;

            animation: blink 1.5s infinite;
        }

        @keyframes blink {

            50% {
                opacity: 0.3;
            }

        }

        /* =========================
           CENTER AI
        ========================= */

        .ai-section {

            position: absolute;

            top: 50%;
            left: 50%;

            transform: translate(-50%, -50%);

            display: flex;

            flex-direction: column;

            align-items: center;

            z-index: 5;
        }

        /* =========================
           ROBOT ORB
        ========================= */

        .robot-wrapper {

            position: relative;

            width: 260px;
            height: 260px;

            display: flex;

            align-items: center;
            justify-content: center;

            opacity: 0;

            transform:
                translateY(100px)
                scale(0.7);

            transition:
                1.5s cubic-bezier(.2,.9,.2,1);

        }

        .robot-wrapper.active {

            opacity: 1;

            transform:
                translateY(0)
                scale(1);

        }

        .robot {

            width: 190px;
            height: 190px;

            border-radius: 50%;

            background:

                radial-gradient(
                    circle at 35% 25%,
                    #d9edff,
                    #7fa7ff 18%,
                    #263b7a 45%,
                    #090d1f 70%
                );

            position: relative;

            display: flex;

            align-items: center;
            justify-content: center;

            box-shadow:

                0 0 40px #416dff,
                0 0 100px rgba(65,109,255,0.7),
                inset 0 0 30px rgba(255,255,255,0.4);

            animation: float 4s infinite ease-in-out;
        }

        /* ROBOT FACE */

        .face {

            width: 145px;
            height: 105px;

            background:

                linear-gradient(
                    145deg,
                    #030817,
                    #111a3b
                );

            border-radius: 55px;

            display: flex;

            align-items: center;
            justify-content: space-around;

            box-shadow:

                inset 0 0 20px #000,
                0 0 20px rgba(100,140,255,0.5);
        }

        .eye {

            width: 35px;
            height: 20px;

            border-top:

                7px solid #58a6ff;

            border-radius: 50%;

            box-shadow:

                0 0 15px #58a6ff,
                0 0 30px #58a6ff;
        }

        /* =========================
           FLOATING
        ========================= */

        @keyframes float {

            0%, 100% {

                transform:
                    translateY(0px)
                    rotate(-2deg);

            }

            50% {

                transform:
                    translateY(-18px)
                    rotate(2deg);

            }

        }

        /* =========================
           ORBIT
        ========================= */

        .orbit {

            position: absolute;

            width: 260px;
            height: 260px;

            border-radius: 50%;

            border:

                1px solid
                rgba(100,130,255,0.4);

            animation: rotate 12s linear infinite;
        }

        .orbit::after {

            content: "";

            position: absolute;

            width: 10px;
            height: 10px;

            background: #7da2ff;

            border-radius: 50%;

            top: 25px;
            left: 30px;

            box-shadow:
                0 0 20px #7da2ff;
        }

        @keyframes rotate {

            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }

        }

        /* =========================
           PLATFORM
        ========================= */

        .platform {

            margin-top: -5px;

            width: 380px;
            height: 75px;

            border-radius: 50%;

            background:

                radial-gradient(
                    ellipse,
                    rgba(130,200,255,0.9),
                    rgba(70,90,255,0.3) 35%,
                    transparent 70%
                );

            filter: blur(2px);

            position: relative;
        }

        .platform::before {

            content: "";

            position: absolute;

            inset: 12px;

            border-radius: 50%;

            border:
                2px solid
                rgba(120,180,255,0.8);

            box-shadow:
                0 0 30px #4f7cff;
        }

        /* =========================
           SPEECH BUBBLE
        ========================= */

        .speech {

            position: absolute;

            left: 250px;
            top: 15px;

            width: 500px;

            padding: 32px 40px;

            border-radius: 28px;

            border:

                1px solid
                rgba(120,150,255,0.6);

            background:

                rgba(5,10,30,0.8);

            backdrop-filter:
                blur(20px);

            box-shadow:

                0 0 50px
                rgba(80,100,255,0.2);

            opacity: 0;

            transform:
                translateX(-30px);

            transition:
                0.8s ease;

        }

        .speech.show {

            opacity: 1;

            transform:
                translateX(0);

        }

        .speech::before {

            content: "";

            position: absolute;

            left: -14px;
            bottom: 25px;

            width: 28px;
            height: 28px;

            background:
                rgba(5,10,30,0.8);

            border-left:
                1px solid
                rgba(120,150,255,0.6);

            border-bottom:
                1px solid
                rgba(120,150,255,0.6);

            transform:
                rotate(45deg);

        }

        .speech p {

            font-size:
                clamp(25px, 3vw, 42px);

            line-height: 1.2;

            font-weight: 600;

            position: relative;

            z-index: 2;
        }

        .highlight {

            color: #7fa8ff;

            font-weight: 800;
        }

        /* =========================
           START BUTTON
        ========================= */

        .start-button {

            position: absolute;

            bottom: 70px;

            left: 50%;

            transform: translateX(-50%);

            padding:
                16px 32px;

            border-radius: 50px;

            border:
                1px solid
                rgba(150,180,255,0.5);

            background:

                rgba(255,255,255,0.05);

            color: white;

            font-family: inherit;

            font-size: 12px;

            letter-spacing: 3px;

            cursor: pointer;

            transition: 0.4s;

            z-index: 20;
        }

        .start-button:hover {

            background:
                rgba(100,130,255,0.2);

            transform:
                translateX(-50%)
                scale(1.05);

            box-shadow:
                0 0 35px
                rgba(80,120,255,0.5);
        }

        .start-button.hide {

            opacity: 0;

            pointer-events: none;

            transform:
                translateX(-50%)
                translateY(30px);
        }

        /* =========================
           BOTTOM TAGLINE
        ========================= */

        .tagline {

            position: absolute;

            bottom: 25px;

            left: 50%;

            transform: translateX(-50%);

            font-size: 11px;

            letter-spacing: 8px;

            color: #7c8496;

            white-space: nowrap;
        }

        /* =========================
           MOBILE
        ========================= */

        @media (max-width: 700px) {

            .header {

                top: 20px;
                left: 20px;
                right: 20px;

            }

            .logo-text {

                font-size: 16px;
                letter-spacing: 5px;

            }

            .logo-sub {

                font-size: 7px;
                letter-spacing: 4px;

            }

            .ai-status {

                font-size: 8px;
                padding: 10px;

            }

            .speech {

                width: 85vw;

                left: 50%;

                top: -130px;

                transform:
                    translateX(-50%)
                    translateY(20px);

            }

            .speech.show {

                transform:
                    translateX(-50%)
                    translateY(0);

            }

            .speech::before {

                display: none;

            }

            .robot-wrapper {

                width: 220px;
                height: 220px;

            }

            .robot {

                width: 160px;
                height: 160px;

            }

            .orbit {

                width: 220px;
                height: 220px;

            }

            .platform {

                width: 300px;

            }

            .tagline {

                font-size: 8px;
                letter-spacing: 4px;

            }

        }

    </style>

</head>

<body>

<div class="container">

    <!-- LIGHTS -->

    <div class="light light-left"></div>

    <div class="light light-right"></div>


    <!-- PARTICLES -->

    <div class="particle" style="top:15%;left:25%;"></div>
    <div class="particle" style="top:25%;left:45%;"></div>
    <div class="particle" style="top:18%;left:70%;"></div>
    <div class="particle" style="top:70%;left:18%;"></div>
    <div class="particle" style="top:65%;left:78%;"></div>
    <div class="particle" style="top:45%;left:10%;"></div>
    <div class="particle" style="top:55%;left:90%;"></div>


    <!-- HEADER -->

    <div class="header">

        <div class="logo">

            <div class="logo-icon">
                ⚡
            </div>

            <div>

                <div class="logo-text">
                    SYNERGY
                </div>

                <div class="logo-sub">
                    SPORTS SOCIETY
                </div>

            </div>

        </div>


        <div class="ai-status">

            <div class="status-dot"></div>

            AI ASSISTANT

            <span>〰〰〰</span>

        </div>

    </div>


    <!-- AI SECTION -->

    <div class="ai-section">


        <!-- ROBOT -->

        <div
            class="robot-wrapper"
            id="robotWrapper"
        >

            <div class="orbit"></div>

            <div class="robot">

                <div class="face">

                    <div class="eye"></div>

                    <div class="eye"></div>

                </div>

            </div>

        </div>


        <!-- PLATFORM -->

        <div class="platform"></div>


        <!-- SPEECH -->

        <div
            class="speech"
            id="speech"
        >

            <p>

                Did I hear

                <br>

                <span class="highlight">
                    Web Development
                </span>

                <br>

                in a Sports Society?

            </p>

        </div>

    </div>


    <!-- BUTTON -->

    <button
        class="start-button"
        id="startButton"
    >

        INITIALIZE AI

    </button>


    <!-- TAGLINE -->

    <div class="tagline">

        WHERE SPORTS MEETS TECHNOLOGY

    </div>


</div>


<script>

    const startButton =
        document.getElementById("startButton");

    const robotWrapper =
        document.getElementById("robotWrapper");

    const speechBox =
        document.getElementById("speech");


    function speakText() {

        const text =
            "Did I hear Web Development in a Sports Society?";


        if ("speechSynthesis" in window) {

            window.speechSynthesis.cancel();


            const speech =
                new SpeechSynthesisUtterance(text);


            speech.rate = 0.9;

            speech.pitch = 1;

            speech.volume = 1;


            const voices =
                window.speechSynthesis.getVoices();


            const preferredVoice =
                voices.find(voice =>
                    voice.name.includes("Google") ||
                    voice.name.includes("Microsoft") ||
                    voice.name.includes("Samantha")
                );


            if (preferredVoice) {

                speech.voice =
                    preferredVoice;

            }


            window.speechSynthesis.speak(speech);

        }

    }


    startButton.addEventListener(
        "click",
        () => {


            startButton.classList.add("hide");


            /* AI ENTERS */

            setTimeout(() => {

                robotWrapper.classList.add(
                    "active"
                );

            }, 300);


            /* SPEECH BUBBLE */

            setTimeout(() => {

                speechBox.classList.add(
                    "show"
                );

            }, 1300);


            /* VOICE ONLY ONCE */

            const alreadyPlayed =
                sessionStorage.getItem(
                    "synergyVoicePlayed"
                );


            if (!alreadyPlayed) {

                setTimeout(() => {

                    speakText();

                    sessionStorage.setItem(
                        "synergyVoicePlayed",
                        "true"
                    );

                }, 1500);

            }

        }

    );


    /* LOAD AVAILABLE VOICES */

    if ("speechSynthesis" in window) {

        window.speechSynthesis.onvoiceschanged =
            () => {

                window.speechSynthesis.getVoices();

            };

    }

</script>

</body>
</html>